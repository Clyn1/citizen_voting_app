name: citizen_voting_app_new
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^6.0.2
  firebase_core: ^4.1.0  # Add this if missing
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1
  image_picker: ^1.0.4
  file_picker: ^10.3.3
  flutter_dotenv: ^6.0.0  # Add this line

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Add assets section for .env file
  assets:
    - .env